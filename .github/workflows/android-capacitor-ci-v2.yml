name: Android Capacitor CI v2

on:
  push:
    branches:
      - android/capacitor
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36
        run: sdkmanager 'platforms;android-36' 'build-tools;36.0.0'

      - name: Install npm dependencies
        run: npm install --no-audit --no-fund

      - name: Generate Android project
        run: npx cap add android

      - name: Configure native Google authentication dependencies
        run: |
          python - <<'PY'
          from pathlib import Path

          variables = Path('android/variables.gradle')
          text = variables.read_text()
          marker = 'ext {'
          if marker not in text:
              raise SystemExit('ext block not found in android/variables.gradle')

          additions = []
          if 'rgcfaIncludeGoogle' not in text:
              additions.append("    rgcfaIncludeGoogle = true")
          if 'androidxCredentialsVersion' not in text:
              additions.append("    androidxCredentialsVersion = '1.3.0'")

          if additions:
              idx = text.find(marker) + len(marker)
              text = text[:idx] + '\n' + '\n'.join(additions) + text[idx:]
              variables.write_text(text)
          PY

      - name: Add Firebase Android configuration
        run: cp android-ci/google-services.json android/app/google-services.json

      - name: Update and sync Capacitor
        run: |
          npx cap update android
          npx cap sync android

      - name: Apply Firebase Crashlytics
        run: |
          python - <<'PY'
          from pathlib import Path

          root_gradle = Path('android/build.gradle')
          text = root_gradle.read_text()
          if 'com.google.firebase:firebase-crashlytics-gradle' not in text:
              marker = "classpath 'com.google.gms:google-services:"
              idx = text.find(marker)
              if idx >= 0:
                  line_end = text.find('\n', idx)
                  text = text[:line_end+1] + "        classpath 'com.google.firebase:firebase-crashlytics-gradle:3.0.7'\n" + text[line_end+1:]
              else:
                  marker = "classpath 'com.android.tools.build:gradle:"
                  idx = text.find(marker)
                  if idx < 0:
                      raise SystemExit('Unable to find root Gradle classpath block')
                  line_end = text.find('\n', idx)
                  text = text[:line_end+1] + "        classpath 'com.google.gms:google-services:4.5.0'\n        classpath 'com.google.firebase:firebase-crashlytics-gradle:3.0.7'\n" + text[line_end+1:]
              root_gradle.write_text(text)

          app_gradle = Path('android/app/build.gradle')
          app = app_gradle.read_text()
          if "apply plugin: 'com.google.firebase.crashlytics'" not in app:
              app += "\napply plugin: 'com.google.firebase.crashlytics'\n"
          if 'firebase-bom:34.17.0' not in app:
              app += "\ndependencies {\n    implementation platform('com.google.firebase:firebase-bom:34.17.0')\n    implementation 'com.google.firebase:firebase-crashlytics'\n    implementation 'com.google.firebase:firebase-messaging'\n}\n"
          app_gradle.write_text(app)
          PY

      - name: Apply Android Integration v3
        run: |
          cp android-ci/MainActivity.java android/app/src/main/java/com/playfootbattle/app/MainActivity.java
          python - <<'PY'
          import xml.etree.ElementTree as ET
          from pathlib import Path

          manifest = Path('android/app/src/main/AndroidManifest.xml')
          android_ns = 'http://schemas.android.com/apk/res/android'
          ET.register_namespace('android', android_ns)
          tree = ET.parse(manifest)
          root = tree.getroot()

          def a(name):
              return f'{{{android_ns}}}{name}'

          target = None
          for activity in root.findall('./application/activity'):
              if activity.get(a('name')) in ('.MainActivity', 'com.playfootbattle.app.MainActivity'):
                  target = activity
                  break
          if target is None:
              raise SystemExit('MainActivity not found in AndroidManifest.xml')

          target.set(a('screenOrientation'), 'portrait')
          target.set(a('windowSoftInputMode'), 'adjustPan')

          has_app_link = False
          for intent_filter in target.findall('intent-filter'):
              for data in intent_filter.findall('data'):
                  if data.get(a('host')) == 'playfootbattle.com':
                      has_app_link = True
                      break

          if not has_app_link:
              app_links = ET.SubElement(target, 'intent-filter')
              app_links.set(a('autoVerify'), 'true')
              action = ET.SubElement(app_links, 'action')
              action.set(a('name'), 'android.intent.action.VIEW')
              category_default = ET.SubElement(app_links, 'category')
              category_default.set(a('name'), 'android.intent.category.DEFAULT')
              category_browsable = ET.SubElement(app_links, 'category')
              category_browsable.set(a('name'), 'android.intent.category.BROWSABLE')
              data = ET.SubElement(app_links, 'data')
              data.set(a('scheme'), 'https')
              data.set(a('host'), 'playfootbattle.com')

          if not any(p.get(a('name')) == 'android.permission.POST_NOTIFICATIONS'
                     for p in root.findall('uses-permission')):
              perm = ET.SubElement(root, 'uses-permission')
              perm.set(a('name'), 'android.permission.POST_NOTIFICATIONS')

          tree.write(manifest, encoding='utf-8', xml_declaration=True)
          PY

      - name: Verify Android integration
        run: |
          cat android/variables.gradle
          grep -R "CapacitorFirebaseAuthentication" -n android/settings.gradle android/app/build.gradle android/capacitor.settings.gradle || true
          java -version
          node --version

      - name: Make Gradle wrapper executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --stacktrace

      - name: Print signing fingerprints
        working-directory: android
        run: ./gradlew signingReport

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: footbattle-android-integration-v3
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
