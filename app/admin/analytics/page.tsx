"use client";
import Link from "next/link";import{useEffect,useMemo,useState}from"react";import ActivePresenceCards from"@/components/admin/ActivePresenceCards";
type RangeKey="today"|"7d"|"30d"|"all";type GameAnalyticsRow={gameName:string;started:number;activePlayers:number;completed:number;noActionExit:number;realAbandoned:number;technicalSuspect:number;abandoned:number;inProgress:number;playAgain:number;shared:number;uniqueUsers:number;loggedInUsers:number;anonymousVisitors:number;completionRate:number;averageDurationSeconds:number};type AnalyticsSummary={totalStarted:number;totalActivePlayers:number;totalCompleted:number;totalNoActionExit:number;totalRealAbandoned:number;totalTechnicalSuspect:number;totalAbandoned:number;totalInProgress:number;totalPlayAgain:number;totalShared:number;averageDurationSeconds:number};type Audience={dau:number;wau:number;uniquePlayers:number;anonymousPlayers:number;newUsers:number;platforms:{web:number;android:number;unknown:number}};type AnalyticsResponse={ok?:boolean;error?:string;summary?:AnalyticsSummary;audience?:Audience;games?:GameAnalyticsRow[]};
const OPTIONS:Array<{key:RangeKey;label:string}>=[{key:"today",label:"Bugün"},{key:"7d",label:"Son 7 Gün"},{key:"30d",label:"Son 30 Gün"},{key:"all",label:"Tümü"}],LABELS:Record<string,string>={wordle:"Wordle",guess_the_player:"Guess the Player",super_lig_guess_the_player:"Süper Lig Guess the Player",player_quiz:"Player Quiz",transfer_quiz:"Transfer Quiz",tic_tac_toe:"Futbol Tic Tac Toe",club_nation:"1 Takım 1 Millet",club_clash:"2 Takım 1 Oyuncu",career_path:"Career Path",shooter:"Şutör"};const ES:AnalyticsSummary={totalStarted:0,totalActivePlayers:0,totalCompleted:0,totalNoActionExit:0,totalRealAbandoned:0,totalTechnicalSuspect:0,totalAbandoned:0,totalInProgress:0,totalPlayAgain:0,totalShared:0,averageDurationSeconds:0},EA:Audience={dau:0,wau:0,uniquePlayers:0,anonymousPlayers:0,newUsers:0,platforms:{web:0,android:0,unknown:0}};function n(v:number){return new Intl.NumberFormat("tr-TR").format(v)}function pct(v:number){return `%${new Intl.NumberFormat("tr-TR",{maximumFractionDigits:1}).format(v)}`}function duration(s:number){if(!s)return"-";if(s<60)return`${s} sn`;const m=Math.floor(s/60),r=s%60;return r?`${m} dk ${r} sn`:`${m} dk`}function users(g:GameAnalyticsRow){return g.loggedInUsers+g.anonymousVisitors}function replay(g:GameAnalyticsRow){return g.started?g.playAgain/g.started*100:0}function health(g:GameAnalyticsRow){if(g.started<5)return{label:"Veri az",cls:"text-slate-300 bg-slate-500/10 border-slate-400/20"};const realRate=g.started?g.realAbandoned/g.started*100:0;if(g.completionRate>=70&&realRate<=25)return{label:"İyi",cls:"text-emerald-300 bg-emerald-500/10 border-emerald-400/20"};if(g.completionRate<40||realRate>=50)return{label:"Sorunlu",cls:"text-red-300 bg-red-500/10 border-red-400/20"};return{label:"İncele",cls:"text-yellow-200 bg-yellow-500/10 border-yellow-300/20"}}
export default function AdminAnalyticsPage(){const[range,setRange]=useState<RangeKey>("today"),[loading,setLoading]=useState(true),[error,setError]=useState(""),[summary,setSummary]=useState(ES),[audience,setAudience]=useState(EA),[games,setGames]=useState<GameAnalyticsRow[]>([]),[details,setDetails]=useState(false);useEffect(()=>{void load()},[range]);async function load(){try{setLoading(true);setError("");const r=await fetch(`/api/admin/analytics?range=${range}`,{cache:"no-store"}),x=await r.json()as AnalyticsResponse;if(!r.ok||!x.ok)throw new Error(x.error??"Analytics verileri yüklenemedi.");setSummary(x.summary??ES);setAudience(x.audience??EA);setGames(x.games??[])}catch(e){setError(e instanceof Error?e.message:"Analytics verileri yüklenemedi.")}finally{setLoading(false)}}const sorted=useMemo(()=>[...games].sort((a,b)=>b.started-a.started),[games]),completion=summary.totalStarted?summary.totalCompleted/summary.totalStarted*100:0;return <main className="min-h-screen bg-[#07111f] px-4 py-8 text-white sm:px-6"><div className="mx-auto max-w-7xl"><header className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.22em] text-green-400">FootBattle Admin</p><h1 className="mt-2 text-3xl font-black">Oyun Özeti</h1><p className="mt-2 text-sm text-slate-400">Terk artık oyun hareketine göre ayrılır: hiç oynamadan çıkış ve gerçek yarıda bırakma birbirinden farklıdır.</p></div><div className="flex flex-wrap gap-2"><Link href="/admin/analytics/ranked" className="rounded-xl border border-green-400/30 bg-green-400/10 px-4 py-2.5 text-xs font-black text-green-300">🏆 Ranked</Link>{OPTIONS.map(o=><button key={o.key} onClick={()=>setRange(o.key)} className={`rounded-xl px-4 py-2.5 text-xs font-black ${range===o.key?"bg-green-500 text-[#07111f]":"border border-white/10 bg-white/[.03] text-slate-400"}`}>{o.label}</button>)}</div></header>{error&&<div className="mt-6 rounded-2xl border border-red-500/20 bg-red-500/10 p-4 text-sm font-bold text-red-300">{error}</div>}<ActivePresenceCards/><section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card label="Başlatma" value={n(summary.totalStarted)} sub="Gerçek game_started" loading={loading}/><Card label="Aktif Oynayan" value={n(summary.totalActivePlayers)} sub="En az 1 oyun hareketi" loading={loading}/><Card label="Tamamlama" value={pct(completion)} sub={`${n(summary.totalCompleted)} tamamlandı`} loading={loading}/><Card label="Oynamadan Çıktı" value={n(summary.totalNoActionExit)} sub="Hareket yapmadan 30 dk geçti" loading={loading}/><Card label="Gerçek Terk" value={n(summary.totalRealAbandoned)} sub="Oynadı ama tamamlamadı" loading={loading}/><Card label="Teknik Şüpheli" value={n(summary.totalTechnicalSuspect)} sub="Hata izi bulunan oturum" loading={loading}/><Card label="Devam" value={n(summary.totalInProgress)} sub="30 dakika dolmadı" loading={loading}/><Card label="DAU" value={n(audience.dau)} sub="Bugünkü girişli tekil" loading={loading}/></section><section className="mt-8 overflow-hidden rounded-2xl border border-white/10 bg-white/[.025]"><div className="flex items-center justify-between border-b border-white/10 p-5"><div><h2 className="font-black">Oyun Performansı</h2><p className="mt-1 text-xs text-slate-500">Başlatma → Aktif Oynayan → Tamamlama / Oynamadan Çıktı / Gerçek Terk / Teknik Şüpheli / Devam</p></div><button onClick={()=>void load()} disabled={loading} className="rounded-xl border border-white/10 px-4 py-2 text-xs font-black">Yenile</button></div><div className="overflow-x-auto"><table className="w-full min-w-[1250px] text-left"><thead><tr className="text-xs text-slate-500"><th className="p-4">Durum</th><th>Oyun</th><th>Oyuncu</th><th>Başlatma</th><th>Aktif</th><th>Tamamlandı</th><th>Oynamadan Çıktı</th><th>Gerçek Terk</th><th>Teknik</th><th>Devam</th><th>Tamamlama</th><th>Ort. Süre</th><th>Tekrar</th></tr></thead><tbody>{sorted.map(g=>{const h=health(g);return <tr key={g.gameName} className="border-t border-white/5"><td className="p-4"><span className={`rounded-full border px-2.5 py-1 text-[10px] font-black uppercase ${h.cls}`}>{h.label}</span></td><td className="font-black">{LABELS[g.gameName]??g.gameName}</td><td>{n(users(g))}</td><td>{n(g.started)}</td><td>{n(g.activePlayers)}</td><td>{n(g.completed)}</td><td>{n(g.noActionExit)}</td><td>{n(g.realAbandoned)}</td><td>{n(g.technicalSuspect)}</td><td>{n(g.inProgress)}</td><td className="font-black">{pct(g.completionRate)}</td><td>{duration(g.averageDurationSeconds)}</td><td>{pct(replay(g))}</td></tr>})}</tbody></table></div></section><div className="mt-6 flex justify-center"><button onClick={()=>setDetails(v=>!v)} className="rounded-xl border border-white/10 bg-white/[.035] px-5 py-3 text-xs font-black text-slate-300">{details?"Teknik Detayları Gizle":"Teknik Detayları Göster"}</button></div>{details&&<section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card label="WAU" value={n(audience.wau)} sub="Son 7 gün girişli" loading={loading}/><Card label="Dönemsel Girişli" value={n(audience.uniquePlayers)} sub="Seçili dönem" loading={loading}/><Card label="Dönemsel Guest" value={n(audience.anonymousPlayers)} sub="Tanımlanabilir guest" loading={loading}/><Card label="Android Başlatma" value={n(audience.platforms.android)} sub={`Web ${n(audience.platforms.web)} · Bilinmeyen ${n(audience.platforms.unknown)}`} loading={loading}/></section>}</div></main>}
function Card({label,value,sub,loading}:{label:string;value:string;sub:string;loading:boolean}){return <div className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><p className="text-[10px] font-black uppercase tracking-wide text-slate-500">{label}</p><p className="mt-3 text-3xl font-black">{loading?"...":value}</p><p className="mt-2 text-xs text-slate-500">{sub}</p></div>}
